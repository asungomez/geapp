version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install --pure-lockfile
        - yarn format
        - yarn lint
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
